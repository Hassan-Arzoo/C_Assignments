/*
Write a program to print size of an int , a float, a char and a double type variable
*/
#include<stdio.h>
int main(){
    int a;
    float b;
    char c;
    double d;
    printf("size of a which is int = %d\nsize of b which is float = %d\nsize of c which is char = %d\nsize of d which is double = %d\n",sizeof(a),sizeof(b),sizeof(c),sizeof(d));

}